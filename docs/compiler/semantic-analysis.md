# Semantic analysis (compiler pipeline)

End-to-end pipeline and IL lowering consume the **semantic model** produced by `RoboSharp.Semantics`.

Detailed binding, symbols, bound tree, and diagnostics: [../semantics/README.md](../semantics/README.md).
