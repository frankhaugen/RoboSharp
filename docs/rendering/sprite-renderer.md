# Sprite renderer

Sprite or tile-based UI rendering uses the same contract as ASCII: **`IWorldRenderProjector`** and `RenderTile[,]` from [render-projection.md](render-projection.md), fed by `RobotWorldSnapshot`.

Host projects own textures, layout, and animation. This document is a stub until a concrete host spec is added.
