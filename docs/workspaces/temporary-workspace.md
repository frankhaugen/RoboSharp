# Temporary workspace

A temporary workspace is useful for:

- tutorials
- samples
- “new unsaved file”
- scratchpad mode
- tests

Recommended rule:

- it still has a synthetic root
- it still has a synthetic project file
- it still respects the same artifact layout rules

That avoids creating a second-class pathway.
