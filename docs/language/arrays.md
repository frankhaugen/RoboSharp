# Arrays (syntax)

Surface:

- Type: `type[]` (see [syntax.md](syntax.md))
- Array literals and index expressions in the grammar

Syntax-level story: one collection syntax, taught alongside functions like `count`, `add`, `getLast`, `takeLast` — see [built-in-functions.md](built-in-functions.md).

**Semantic** rules (index type, element typing, contextual typing for `[]`): [../semantics/arrays.md](../semantics/arrays.md).
