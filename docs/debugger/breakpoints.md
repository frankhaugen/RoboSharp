# Breakpoints

Source and IL breakpoints must both be supported.

Source breakpoints resolve to instruction indices via debug metadata.

See [debugger-architecture.md](debugger-architecture.md) for how the Studio keeps source and IL views in sync while debugging.
