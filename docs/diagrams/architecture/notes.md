# Architecture notes (nested docs sample)

This file exists under `docs/diagrams/architecture/` so the solution updater can prove **two levels of nesting** under `docs/` (`/docs/diagrams/` → `/docs/diagrams/architecture/`).

See [layer-map.md](../layer-map.md) for the teaching-oriented layer diagram.
